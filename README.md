# 🚀 TorrentFlow - Guía de Instalación en Ubuntu Server

Este documento explica cómo instalar y ejecutar TorrentFlow utilizando Docker.

## 1. Requisitos Previos
Asegúrate de tener instalados Docker y Docker Compose en tu servidor Ubuntu:
```bash
sudo apt update
sudo apt install docker.io docker-compose -y
sudo systemctl enable --now docker